# ✅ Создание нового покупателя - Руководство

**Дата:** 17 ноября 2025

---

## Что было сделано

### 1. Создан компонент CreateCustomerModal

**Расположение:** [src/shared/ui/CreateCustomerModal/](src/shared/ui/CreateCustomerModal/)

Компонент представляет собой модальное окно для создания нового покупателя с полной валидацией.

### 2. Интеграция с CustomerSearch

Теперь при поиске клиента, если клиент не найден, появляется кнопка **"+ Bu telefon bilan yangi mijoz yaratish"**

---

## Как это работает

### Сценарий 1: Клиент не найден при поиске

1. **Кассир вводит номер телефона** в поле поиска (`+998901234567`)
2. **Система ищет клиента** по номеру
3. **Если не найден** - показывается dropdown с сообщением:
   ```
   ┌────────────────────────────────────┐
   │       Mijoz topilmadi              │
   │                                    │
   │  ┌──────────────────────────────┐ │
   │  │ + Bu telefon bilan yangi     │ │
   │  │   mijoz yaratish             │ │
   │  └──────────────────────────────┘ │
   └────────────────────────────────────┘
   ```

4. **Кассир кликает на кнопку** "Yangi mijoz yaratish"
5. **Открывается модальное окно** с формой создания клиента
6. **Номер телефона автоматически заполнен** из поля поиска

---

## Модальное окно создания клиента

### Поля формы

#### Обязательные поля:
- **Ism** (Имя) - обязательное
- **Telefon** - обязательное, автоматически заполняется

#### Опциональные поля:
- **Familiya** (Фамилия)
- **Email**
- **Manzil** (Адрес)
- **Izoh** (Примечания)

#### Тип клиента (radio buttons):
- **Jismoniy shaxs** (Физическое лицо) - по умолчанию
- **Yuridik sh axs** (Юридическое лицо)

#### Если выбрано "Yuridik shaxs":
- **Tashkilot nomi** (Название организации) - обязательное
- **INN** - опционально

#### Дополнительно:
- **VIP mijoz** (checkbox) - отметить, если клиент VIP

---

## Скриншоты формы

### Физическое лицо (Jismoniy shaxs)

```
┌─────────────────────────────────────────────┐
│  Yangi mijoz qo'shish                   [X] │
├─────────────────────────────────────────────┤
│                                             │
│  Mijoz turi:                                │
│  ⦿ Jismoniy shaxs   ○ Yuridik shaxs        │
│                                             │
│  Ism: *                                     │
│  ┌───────────────────────────────────────┐ │
│  │ Ismni kiriting                        │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  Familiya:                                  │
│  ┌───────────────────────────────────────┐ │
│  │ Familiyani kiriting                   │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  Telefon: *                                 │
│  ┌───────────────────────────────────────┐ │
│  │ +998901234567                         │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  Email:                                     │
│  ┌───────────────────────────────────────┐ │
│  │ email@example.com                     │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  Manzil:                                    │
│  ┌───────────────────────────────────────┐ │
│  │ Manzilni kiriting                     │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  Izoh:                                      │
│  ┌───────────────────────────────────────┐ │
│  │ Qo'shimcha ma'lumot                   │ │
│  │                                       │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  ☑ VIP mijoz                                │
│                                             │
├─────────────────────────────────────────────┤
│              [Saqlash]                      │
└─────────────────────────────────────────────┘
```

### Юридическое лицо (Yuridik shaxs)

```
┌─────────────────────────────────────────────┐
│  Yangi mijoz qo'shish                   [X] │
├─────────────────────────────────────────────┤
│                                             │
│  Mijoz turi:                                │
│  ○ Jismoniy shaxs   ⦿ Yuridik shaxs        │
│                                             │
│  Ism: *                                     │
│  ┌───────────────────────────────────────┐ │
│  │ Ismni kiriting                        │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  ... (другие поля)                          │
│                                             │
│  Tashkilot nomi: *                          │
│  ┌───────────────────────────────────────┐ │
│  │ Tashkilot nomini kiriting             │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  INN:                                       │
│  ┌───────────────────────────────────────┐ │
│  │ INN raqamini kiriting                 │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  ... (остальные поля)                       │
│                                             │
└─────────────────────────────────────────────┘
```

---

## Валидация

Компонент автоматически проверяет:

### ✅ Обязательные поля:
- **Ism** (Имя) - не может быть пустым
- **Telefon** - должен быть 13 символов (`+998XXXXXXXXX`)
- **Tashkilot nomi** - если выбрано "Yuridik shaxs"

### 🔧 Автоматическое форматирование:
- **Телефон** - автоматически добавляется `+998`
- **Телефон** - только цифры и `+`
- **Телефон** - максимум 13 символов

### ❌ Ошибки:
Если валидация не пройдена, показывается уведомление с текстом ошибки:
- "Iltimos, mijoz ismini kiriting"
- "Telefon raqam noto'g'ri formatda (+998XXXXXXXXX)"
- "Iltimos, tashkilot nomini kiriting"

---

## Что происходит после создания

### 1. Клиент создается на backend
```typescript
POST /api/customers/customers/
{
  "first_name": "Иван",
  "last_name": "Петров",
  "phone": "+998901234567",
  "email": "ivan@example.com",
  "customer_type": "individual",
  "is_vip": false,
  // ... и т.д.
}
```

### 2. Автоматически выбирается
После успешного создания:
- Модальное окно закрывается
- Новый клиент **автоматически выбирается** в CustomerSearch
- Отображается карточка клиента с информацией
- Можно сразу продолжить продажу

### 3. Готов к использованию
Теперь этот клиент:
- ✅ Сохранен в базе данных
- ✅ Можно найти при следующих продажах
- ✅ Будет накапливать статистику покупок
- ✅ Получит бонусы (если настроено)

---

## Использование в коде

### Пример интеграции в другие компоненты:

```typescript
import { useState } from "react";
import { CreateCustomerModal } from "@/shared/ui/CreateCustomerModal";
import type { Customer } from "@/entities/customer/api/types";

export const YourComponent = () => {
  const [showModal, setShowModal] = useState(false);
  const [customer, setCustomer] = useState<Customer | null>(null);

  return (
    <div>
      <button onClick={() => setShowModal(true)}>
        Yangi mijoz qo'shish
      </button>

      <CreateCustomerModal
        isOpen={showModal}
        onClose={() => setShowModal(false)}
        onSuccess={(newCustomer) => {
          setCustomer(newCustomer);
          setShowModal(false);
          console.log("Yangi mijoz yaratildi:", newCustomer);
        }}
        initialPhone="+998"
      />

      {customer && (
        <p>Tanlangan: {customer.first_name}</p>
      )}
    </div>
  );
};
```

---

## Props компонента CreateCustomerModal

```typescript
interface CreateCustomerModalProps {
  // Открыто ли модальное окно
  isOpen: boolean;

  // Callback для закрытия
  onClose: () => void;

  // Callback при успешном создании (возвращает нового клиента)
  onSuccess: (customer: Customer) => void;

  // Начальное значение телефона (по умолчанию "+998")
  initialPhone?: string;
}
```

---

## Возможности

### ✅ Что может компонент:

1. **Создание физических лиц** - обычные покупатели
2. **Создание юридических лиц** - компании с ИНН
3. **VIP статус** - отметка VIP клиентов
4. **Валидация полей** - проверка обязательных полей
5. **Автоматический выбор** - выбирает созданного клиента
6. **Обработка ошибок** - показывает уведомления об ошибках
7. **Блокировка повторной отправки** - кнопка блокируется во время создания
8. **Очистка формы** - после успешного создания форма очищается

---

## Backend интеграция

Компонент использует хук `useCreateCustomer`, который отправляет:

```typescript
POST /api/customers/customers/
Content-Type: application/json

{
  "first_name": "string",        // обязательно
  "last_name": "string",         // опционально
  "phone": "+998XXXXXXXXX",      // обязательно
  "email": "email@example.com",  // опционально
  "customer_type": "individual", // или "organization"
  "organization_name": "string", // если organization
  "inn": "string",               // опционально
  "address": "string",           // опционально
  "notes": "string",             // опционально
  "is_vip": false,               // boolean
  "group": null                  // ID группы (опционально)
}
```

Ответ:
```typescript
{
  "id": 1,
  "first_name": "Иван",
  "last_name": "Петров",
  "phone": "+998901234567",
  // ... остальные поля
  "created_at": "2025-11-17T12:00:00Z",
  "updated_at": "2025-11-17T12:00:00Z"
}
```

---

## Тестирование

### Checklist для тестирования:

- [ ] Открыть страницу кассы
- [ ] Ввести несуществующий номер телефона
- [ ] Нажать на кнопку "Yangi mijoz yaratish"
- [ ] Проверить, что телефон автоматически заполнен
- [ ] Заполнить обязательные поля (Ism)
- [ ] Нажать "Saqlash"
- [ ] Проверить, что клиент создан
- [ ] Проверить, что клиент автоматически выбран
- [ ] Проверить карточку клиента
- [ ] Переключить на "Yuridik shaxs"
- [ ] Проверить, что появились поля организации
- [ ] Попробовать сохранить без названия организации
- [ ] Проверить валидацию
- [ ] Заполнить и сохранить юр. лицо
- [ ] Проверить, что всё работает

---

## Файлы компонента

1. **[src/shared/ui/CreateCustomerModal/index.tsx](src/shared/ui/CreateCustomerModal/index.tsx)** - Основной компонент
2. **[src/shared/ui/CreateCustomerModal/CreateCustomerModal.module.scss](src/shared/ui/CreateCustomerModal/CreateCustomerModal.module.scss)** - Стили

---

## Кастомизация стилей

Можно изменить стили через SCSS модули:

```scss
// Ваши стили
.customModal {
  :global {
    .form {
      gap: 20px; // Больше расстояние между полями
    }

    .input {
      border-color: #your-color;
      font-size: 16px;
    }

    .required {
      color: #ff0000;
    }
  }
}
```

---

## Статус: ✅ Готово к использованию!

Теперь на странице кассы:
1. ✅ Поиск клиента работает
2. ✅ Если не найден - показывается кнопка создания
3. ✅ Модальное окно с формой создания
4. ✅ Валидация полей
5. ✅ Автоматический выбор после создания
6. ✅ Готов к использованию в продаже

**Попробуйте создать клиента прямо сейчас!**

---

**Дата создания:** 17 ноября 2025
**Автор:** Claude Code
**Версия:** 1.0
