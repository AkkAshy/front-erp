# 🚀 Быстрый старт - ERP система

## За 5 минут до работающей системы

### 1️⃣ Запуск Backend (1 минута)

```bash
cd /Users/akkanat/Projects/erp_v2/new_backend
source venv/bin/activate
python manage.py runserver
```

✅ Backend запущен на `http://localhost:8000`

---

### 2️⃣ Запуск Frontend (1 минута)

```bash
cd /Users/akkanat/Projects/erp_v2/new_frontend
npm run dev
```

✅ Frontend запущен на `http://localhost:3110`

---

### 3️⃣ Первый вход (3 минуты)

#### Вариант А: Регистрация нового магазина

1. Открыть `http://localhost:3110`
2. Перейти на страницу регистрации
3. Заполнить форму:

```
Личные данные:
  Имя: Иван
  Фамилия: Петров
  Телефон: +998 90 123 45 67

Вход:
  Логин: ivan_shop
  Пароль: Pass123!
  Подтверждение: Pass123!

Магазин:
  Название: Супермаркет Азия
  Адрес: ул. Навои 45
  Город: Ташкент
  Телефон: +998 71 234 56 78
```

4. Нажать "Ro'yxatdan o'tish"

✅ Автоматически:
- Создан магазин
- Создан владелец
- Сгенерирован `tenant_key`
- Выполнен автологин
- Редирект на Dashboard

#### Вариант Б: Тестовый вход

Если бэкенд уже имеет тестовые данные:

```
Логин: admin
Пароль: admin123
```

---

### 4️⃣ Быстрая проверка

#### Проверка 1: tenant_key сохранен

Открыть консоль браузера (F12) и выполнить:

```javascript
localStorage.getItem('tenant_key')
// Должен вернуть что-то вроде: "asia_market_a3f4b2c1"
```

✅ Если видите ключ - мультитенантность работает!

#### Проверка 2: Создать товар

1. Перейти в "Tovarlar" (Товары)
2. Нажать "+ Yangi tovar"
3. Заполнить:
   - Название: Молоко
   - Категория: (выбрать любую или создать)
   - Единица: шт
   - Себестоимость: 8000
   - Цена продажи: 12000
   - Количество: 100
4. Нажать "Yaratish"

✅ Товар создан с автоматически сгенерированным SKU и штрихкодом!

#### Проверка 3: Создать продажу

1. Перейти на главную (POS)
2. Открыть смену (начальная сумма: 100000)
3. Добавить товар "Молоко" - 2 шт
4. Оплата: Наличные, получено: 50000
5. Нажать "Sotish"

✅ Продажа создана, чек сгенерирован, сдача рассчитана!

---

## 📋 Основные функции

### Товары
- **Создать товар** - SKU и штрихкод генерируются автоматически
- **Партии** - создаются автоматически при создании товара
- **Сканирование** - поиск по штрихкоду
- **Остатки** - автоматическое отслеживание

### Продажи (POS)
- **Открыть смену** - перед началом работы
- **Создать продажу** - номер чека генерируется автоматически
- **Способы оплаты** - наличные, карта, перевод, комбинированная
- **Сдача** - рассчитывается автоматически
- **Закрыть смену** - с отчетом

### Категории
- Создание категорий
- Иерархия (родитель-дочка)
- Фильтрация товаров по категориям

### Единицы измерения
- Создание единиц (шт, кг, л, м и т.д.)
- Краткое название (шт, kg, l)

---

## 🔥 Полезные команды

### Проверка статуса

```bash
# Backend
curl http://localhost:8000/api/users/profile/ \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "X-Tenant-Key: YOUR_TENANT_KEY"

# Проверка товаров
curl http://localhost:8000/api/products/products/ \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "X-Tenant-Key: YOUR_TENANT_KEY"
```

### Очистка localStorage (если нужно)

```javascript
// В консоли браузера
localStorage.clear()
// Затем перезагрузить страницу
```

---

## ❓ Частые вопросы

### Q: Где tenant_key?

A: Автоматически сохраняется в `localStorage` после регистрации/логина

```javascript
localStorage.getItem('tenant_key')
```

### Q: Где посмотреть токен?

A: В `localStorage`:

```javascript
localStorage.getItem('access_token')
localStorage.getItem('refresh_token')
```

### Q: Не создается товар

A: Проверьте:
1. Категория существует?
2. Единица измерения существует?
3. Вы НЕ отправляете `sku` или `barcode`? (они генерируются автоматически)

### Q: Не создается продажа

A: Проверьте:
1. Открыта ли смена?
2. Есть ли товары в наличии?
3. Указан ли `session` (ID смены)?

### Q: Как открыть смену?

A: На главной странице (POS) появится модальное окно, если смена не открыта

---

## 🎯 Что дальше?

После быстрого старта, изучите:

1. **[API_UPDATED.md](API_UPDATED.md)** - полное описание API
2. **[TESTING_GUIDE.md](TESTING_GUIDE.md)** - детальное тестирование
3. **[SUMMARY_RU.md](SUMMARY_RU.md)** - резюме изменений

---

## 💡 Подсказки

### Автогенерация на бэкенде

**НЕ отправляйте** с фронтенда:
- `sku` - генерируется автоматически
- `barcode` - генерируется автоматически
- `batch_number` - генерируется автоматически
- `receipt_number` - генерируется автоматически

### Один запрос для создания товара

```typescript
// ✅ Правильно - все в одном запросе
await productApi.create({
  name: "Молоко",
  category: 1,
  unit: 1,
  cost_price: 8000,
  sale_price: 12000,
  initial_quantity: 100  // Первая партия создается автоматически
})

// ❌ Неправильно - два запроса
await productApi.create({ name: "Молоко", barcode: "123" })
await productApi.addBatch({ product: 1, quantity: 100 })
```

### Мультитенантность

Каждый магазин имеет свой `tenant_key`. Данные полностью изолированы:
- Товары
- Продажи
- Пользователи
- Категории
- Все остальное

---

## 🎉 Готово!

**Система запущена и работает!**

- ✅ Backend: `http://localhost:8000`
- ✅ Frontend: `http://localhost:3110`
- ✅ Multi-tenant: работает автоматически
- ✅ Автогенерация: SKU, штрихкоды, чеки

**Можно пользоваться!** 🚀

---

_Быстрый старт - ERP система_
_Время запуска: ~5 минут_
